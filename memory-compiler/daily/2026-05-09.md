# Daily Log: 2026-05-09

## Sessions

## Memory Maintenance

### Session (15:19)

**Context:** User completing three UX fixes for T3Code Electron app: onboarding flow, connection status visibility, and keybindings editor

**Key Exchanges:**

- Discovered `server.refreshProviders` RPC already exists for re-checking provider installations
- Both project creation paths route through `createProjectFromPath` requiring `nativeApi`
- `ResolvedKeybindingRule` only has `command`, `shortcut`, `whenAst` — not raw `key`/`when` strings, required conflict detection adjustment
- Pre-existing typecheck errors exist in `apps/server` (preview, orchestration, http, providers) but `apps/web` passes clean

**Decisions Made:**

- Inserted Project step at position 1 in onboarding (Provider→2, Mobile→3, Git→4, Import→5)
- Exported `TOTAL_ONBOARDING_STEPS` constant as single source of truth
- Bumped WS reconnect delay 50ms→1500ms so banner is visible before reload
- Scoped Fix #2 to connection-state visibility only; resume-fallback toast deferred (needs new server-side domain event)

**Lessons Learned:**

- Zustand stores need imperative `setConnectionStatus()` setter for non-React call sites (WS transport)
- Route registration in `routeTree.gen.ts` requires 10 surgical patches across FileRoutes maps, union types, registry block, and route children
- Keybindings system uses `STATIC_KEYBINDING_COMMANDS` from `packages/contracts/src/keybindings.ts` and `upsertKeybinding` RPC

**Action Items:**

- Resume-fallback toast needs `session/threadResumeFallback` client-visible domain event in `packages/contracts`
- Three subagent deep-dives (provider auth, diff review, keybindings system) ready for next round of fixes

### Memory Flush (19:49)

FLUSH_ERROR ts=2026-05-09T17:49:07.443727+00:00 type=Exception msg=Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details | traceback_tail='Traceback (most recent call last):\n File "<repo>/scripts/flush.py", line 229, in run_flush\n async for message in query(\n File "<repo>/.venv/lib/python3.12/site-packages/claude_agent_sdk/query.py", line 123, in query\n async for message in client.process_query(\n File "<repo>/.venv/lib/python3.12/site-packages/claude_agent_sdk/\_internal/client.py", line 120, in process_query\n async for data in query.receive_messages():\n File "<repo>/.venv/lib/python3.12/site-packages/claude_agent_sdk/\_internal/query.py", line 602, in receive_messages\n raise Exception(message.get("error", "Unknown error"))\nException: Command failed with exit code 1 (exit code: 1)\nError output: Check stderr output for details\n'

### Session (20:34)

**Context:** User applied 14 code review findings across a full-stack monorepo (React web, Node.js server, iOS mobile, Python compiler scripts)

**Key Exchanges:**

- Reviewed 14 distinct bug/security/correctness findings and applied fixes to 11 files across 4 apps
- Typechecked both web and server apps; server had pre-existing Effect v4 API errors unrelated to changes

**Decisions Made:**

- Info.plist: Chose to remove `UIRequiresFullScreen` (Option A) to preserve multi-window/scene support rather than flipping `UIApplicationSupportsMultipleScenes` to false
- `addGroup`: Changed to return `null` on blank input rather than throwing, matching existing `addTodo` pattern
- Cross-platform watcher: Implemented recursive directory walk with individual watchers rather than adding chokidar dependency — kept change self-contained
- Double-click suppression: Used per-item timer Map in a ref rather than extracting separate component — minimized diff scope

**Lessons Learned:**

- `fs.watch({recursive: true})` only works on macOS/darwin; Linux requires walking subdirs and attaching individual watchers
- Path traversal checks: comparing `resolve(input) === resolve(input)` is always true; must compare raw vs resolved
- React click/double-click conflict needs setTimeout suppression pattern with per-element timers
- Branded TypeScript IDs (`ProjectGroupId`) shouldn't use empty string as sentinel — use proper `null`
- Python async stream errors returned as dicts bypass `except` blocks unless explicitly raised
- When catch block shows toast then returns, caller can't distinguish success/failure — rethrow to preserve error state

**Action Items:**

- None explicit — all 14 fixes committed in two commits (`39ecd4ca`, `6a9f4149`)

### Session (20:34)

**Context:** Comprehensive security and reliability audit of Bird Code application across onboarding, provider/chat lifecycle, diff/git/checkpointing, preview/dev-server, and mobile/tunnel systems.

**Key Exchanges:**

- Parallel investigation by 5 agents identified 27 bugs ranked by user impact
- Critical findings centered on authentication/authorization flaws in mobile pairing and WebSocket handling
- Major data integrity issues in checkpoint/revert system and import deduplication

**Decisions Made:**

- Prioritize C1+C2 together: redesign pairing to use per-device WS-scoped tokens instead of sharing master auth token via QR
- Prioritize C3+C4 together: fix WS subscribe race and pending request drain to eliminate "stuck mid-turn" issues
- Gate SPA + preview proxy behind auth before more tunnel adoption (H1)
- Fix baseline-required + clean -fd safety + dirty-buffer flush together (C5+H3) to prevent data loss on revert

**Lessons Learned:**

- QR pairing antipattern: master auth token in QR payload = any photo = full admin access; use short-lived pairing nonce → per-device token exchange
- WS subscribe race pattern: must subscribe to PubSub BEFORE historical replay to avoid event gaps (`Effect.acquireRelease`)
- `git clean -fd` in revert path destroys untracked files (.env.local, notes); need to snapshot untracked list at baseline
- Origin header rewriting matters for HMR: Vite/Next 14+ reject when Origin doesn't match Host
- Process cleanup on restart: must await actual process exit before deleting from session maps (EADDRINUSE bugs)
- Import deduplication essential: lookup by workspaceRoot or (provider, sessionId) before creating new records

**Action Items:**

- Redesign mobile pairing: short-lived nonce → per-device WS-scoped token (C1+C2)
- Fix WS subscribe race: subscribe before replay, add gap timer to reorder buffer (C3)
- Drain pending requests on subprocess crash using existing stopSession helper (C4)
- Add auth to tunnel-exposed SPA and /preview/\* proxy (H1)
- Require baseline capture before turn start; add turn.cancelled checkpoint path (C5)
- Add confirm dialog before revert when dirty buffers exist; broadcast workspace.invalidate after restore (H3)
- Dedupe imports by workspaceRoot / (provider, sessionId) (C6)
- Fix HMR Origin header rewrite in preview WebSocket upgrade (H7)
- Add self-only constraint to device revocation (H2)
- Handle review failure states in GitActionsControl to prevent stuck push (H4)

### Memory Flush (21:29)

FLUSH_ERROR ts=2026-05-09T19:29:30.427240+00:00 type=Exception msg=Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details | traceback_tail='Traceback (most recent call last):\n File "<repo>/scripts/flush.py", line 229, in run_flush\n async for message in query(\n File "<repo>/.venv/lib/python3.12/site-packages/claude_agent_sdk/query.py", line 123, in query\n async for message in client.process_query(\n File "<repo>/.venv/lib/python3.12/site-packages/claude_agent_sdk/\_internal/client.py", line 120, in process_query\n async for data in query.receive_messages():\n File "<repo>/.venv/lib/python3.12/site-packages/claude_agent_sdk/\_internal/query.py", line 602, in receive_messages\n raise Exception(message.get("error", "Unknown error"))\nException: Command failed with exit code 1 (exit code: 1)\nError output: Check stderr output for details\n'

### Memory Flush (21:29)

FLUSH_ERROR ts=2026-05-09T19:29:36.375423+00:00 type=Exception msg=Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details | traceback_tail='Traceback (most recent call last):\n File "<repo>/scripts/flush.py", line 229, in run_flush\n async for message in query(\n File "<repo>/.venv/lib/python3.12/site-packages/claude_agent_sdk/query.py", line 123, in query\n async for message in client.process_query(\n File "<repo>/.venv/lib/python3.12/site-packages/claude_agent_sdk/\_internal/client.py", line 120, in process_query\n async for data in query.receive_messages():\n File "<repo>/.venv/lib/python3.12/site-packages/claude_agent_sdk/\_internal/query.py", line 602, in receive_messages\n raise Exception(message.get("error", "Unknown error"))\nException: Command failed with exit code 1 (exit code: 1)\nError output: Check stderr output for details\n'

### Memory Flush (21:30)

FLUSH_ERROR ts=2026-05-09T19:30:28.806388+00:00 type=Exception msg=Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details | traceback_tail='Traceback (most recent call last):\n File "<repo>/scripts/flush.py", line 229, in run_flush\n async for message in query(\n File "<repo>/.venv/lib/python3.12/site-packages/claude_agent_sdk/query.py", line 123, in query\n async for message in client.process_query(\n File "<repo>/.venv/lib/python3.12/site-packages/claude_agent_sdk/\_internal/client.py", line 120, in process_query\n async for data in query.receive_messages():\n File "<repo>/.venv/lib/python3.12/site-packages/claude_agent_sdk/\_internal/query.py", line 602, in receive_messages\n raise Exception(message.get("error", "Unknown error"))\nException: Command failed with exit code 1 (exit code: 1)\nError output: Check stderr output for details\n'
