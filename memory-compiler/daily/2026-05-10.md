# Daily Log: 2026-05-10

## Sessions

## Memory Maintenance

### Session (15:49)

**Context:** Large-scale bugfix sweep across server, web, desktop, and mobile apps — 21 surgical fixes organized in 4 waves.

**Key Exchanges:**
- Systematic validation-then-fix pattern: validated each issue against codebase before applying fix
- Wave organization: Server data integrity → Preview/proxy → Web → Desktop/iOS
- Type breaks encountered during H6 (WS reconnect signal) — solved by specializing orchestration onDomainEvent type rather than broadening base type

**Decisions Made:**
- M3 (gitStatus refetch): Chose defensive `isFetching` guard over deeper refetch restructuring since server cache already invalidated by mutation
- M10 (iOS WebView re-pair): Added explanatory text rather than auto-recovery logic since Disconnect button already provides the path
- H9 (cloudflared zombies): Implemented PID file + reap-on-next-launch pattern for daemon cleanup
- Skipped Vercel Workflow consideration for H4 — local Effect orchestration is the relevant pattern

**Lessons Learned:**
- Terminal failure states in orchestration: `"interrupted"` and `"error"` (not just `"error"`)
- Swift diagnostics are env-only, not real build failures
- 22 TS errors are baseline pre-existing (http.ts, decider.ts, GeminiAdapter.ts, integration test)
- Pre-existing lint warnings on tunnelManager lines 179, 218 are in download path — don't chase

**Action Items:**
- Run `bun run test` — especially ProviderCommandReactor.test.ts (H5), appDetection.test.ts (M7), wsNativeApi.test.ts (H6)
- Smoke test: open project → send turn → commit+push → open preview → trigger onboarding
- Architectural items pending design docs: C1+C2 (per-device WS tokens), C3 (subscribe-attach race), C5+H3 (revert path data-loss), H1 (SPA preview auth), M6 (standalone path traversal)

### Session (15:49)

**Context:** Large UX audit implementation across the t3code codebase - 15 files modified, 1 created, implementing empty states, onboarding improvements, permission controls, and ChatView decomposition.

**Key Exchanges:**
- Phase 1 completed with syntax error in OnboardingSheet.tsx:176 requiring immediate fix
- Holistic verification needed between phases because multiple agents touched same files (ChatView.tsx, Sidebar.tsx)
- Agent G handled empty state audit + keyboard nav across Sidebar, SkillsManager, SearchModal, ChatView
- Agent H decomposed ChatView's empty draft view into EmptyDraftThreadView.tsx (5916→5738 lines, -178 lines)

**Decisions Made:**
- S2 (approval modal) superseded by H2 split-button + demoted menu approach
- S4 (decompose ChatView) only partially completed - full decomposition deferred as multi-day effort; first extraction shipped
- S6 (workflow library/saved prompts) deferred - substantial new feature requiring backing store + edit UI
- S3 (3+2 onboarding) deferred - needs product call on mandatory steps
- FilesPanel empty states deferred (low priority, deeply inline per-section messages)
- AutomationsManager + PreviewPanel empty states skipped - already implemented canonical pattern

**Lessons Learned:**
- Empty state canonical pattern: 3-part with `text-muted-foreground/{50,40,30}` hierarchy (heading + reason + action hint)
- Keyboard nav fix for project-switcher: let ArrowUp/ArrowDown/Enter bubble to base-ui Menu while stopping character keys
- Icon pruning reduces lint warnings (54→54 maintained, 4 cleared by cleanup)
- Turbo cache can be stale - run lint + fmt cumulatively after multi-agent file touches

**Action Items:**
- Manual smoke test in desktop app: onboarding, BranchToolbar permissions toggle, approval flow, 9 suggestion cards, Reset filters pill
- Pre-existing typecheck errors in apps/server, t3 integration tests, apps/marketing remain unaddressed
- Full ChatView decomposition remains (multiple extractions beyond EmptyDraftThreadView)
- S6 workflow library needs backing store + edit UI spec

### Session (23:13)

**Context:** Multi-agent UX overhaul across t3code web app, executing a prioritized audit (H1-H5 high-priority, M1-M9 medium, L1-L5 low, S1-S7 speculative)

**Key Exchanges:**
- Phase 1 agents (6 parallel) completed with stale turbo cache issue requiring holistic verify
- OnboardingSheet.tsx:176 syntax error discovered and fixed between phases
- Phase 2/3 agents (G: empty state audit + keyboard nav, H: ChatView decomposition) dispatched and completed

**Decisions Made:**
- S2 approval modal superseded by H2 split-button + demoted menu approach
- S4 ChatView decomposition done partially (−178 lines via EmptyDraftThreadView extraction); full decomposition deferred as multi-day effort
- S6 workflow library deferred — substantial new feature requiring separate spec
- S3 (3+2 onboarding) deferred — needs product call on which steps mandatory
- AutomationsManager + PreviewPanel empty states skipped (already good); FilesPanel deferred (low priority)

**Lessons Learned:**
- Turbo cache can go stale when multiple agents touch same files — always run holistic verify between phases
- Canonical empty-state pattern: `text-muted-foreground/{50,40,30}` hierarchy for heading/reason/action
- Project-switcher keyboard nav fix: let ArrowUp/ArrowDown/Enter bubble to base-ui Menu while stopping character keys
- When extracting components, prune unused imports (cleared 4 lint warnings by removing 10 lucide icons)

**Action Items:**
- Manual smoke test in desktop app: onboarding, BranchToolbar permissions toggle, approval flow, 9 suggestion cards, Reset filters pill
- Future: S3 (3+2 onboarding) after product call
- Future: S4 full ChatView decomposition (multi-day)
- Future: S6 workflow library spec and implementation

