# Daily Log: 2026-04-09

## Sessions

## Memory Maintenance

### Session (20:01)

**Context:** Integrated Claude Memory Compiler into project for automatic conversation logging, knowledge base compilation, and context injection across sessions.

**Key Exchanges:**
- Clarified 3-stage pipeline: SessionStart (context injection) → SessionEnd/PreCompact (transcript capture) → compile.py (daily log → knowledge articles)
- Resolved path resolution issues: Python scripts use `Path(__file__).resolve().parent.parent` to locate ROOT; hook execution context matters
- Verified venv isolation: `uv run --directory memory-compiler` correctly scopes dependencies to `memory-compiler/.venv`, avoiding project root pollution
- Tested SessionStart hook successfully injects knowledge context at session initialization

**Decisions Made:**
- Isolated memory compiler in `memory-compiler/` subdirectory to avoid naming conflicts with existing `/scripts` directory
- Used `uv run --directory memory-compiler` pattern for hook subprocess calls to scope Python environment properly
- Configured compile.py auto-trigger at 6 PM if daily log changed (avoids excessive API calls)
- Set venv within memory-compiler/ rather than project root (cleaner isolation)

**Lessons Learned:**
- macOS subprocess detachment: `start_new_session=True` in Popen detaches; without it still works when spawned from hooks (design choice in upstream)
- Hook working directory: SessionEnd hook spawns flush.py from project root; flush.py then spawns compile.py with start_new_session=True
- Knowledge index format: knowledge/index.md holds concept links; knowledge/log.md tracks daily log references (used for recall)

**Action Items:**
- Manual cleanup: delete stray empty directories at project root (`rm -rf daily knowledge reports`)
- Start new session to trigger SessionStart hook and begin automatic pipeline
- Monitor costs: flush ~$0.02-0.05/session, compilation ~$0.45-0.65/daily log
- Optional: run `uv run --directory memory-compiler python scripts/lint.py --structural-only` periodically to check knowledge base health

### Session (22:17)

**Context:** Completed full A2A (Agent-to-Agent) protocol integration into Bird Code across 8 systematic phases covering contracts, database, server services, client layer, provider adapter, RPC handlers, web UI, and authentication.

**Key Exchanges:**
- All 8 phases implemented following Bird Code's existing Effect Services + Layers pattern
- Multiple TypeCheck validations performed throughout; only pre-existing ChatView.browser.tsx errors remain (Playwright-related, unrelated to A2A work)
- Contracts, shared, scripts, desktop, marketing, and server packages all compile cleanly

**Decisions Made:**
- A2A HTTP endpoint includes auth middleware supporting Bearer tokens and API keys via `A2A_INBOUND_AUTH_TOKEN` env var
- Agent card endpoint stays public per A2A spec (discovery endpoint)
- JSON-RPC endpoint requires authentication when configured
- A2a adapter implemented as full `ProviderAdapterShape` following same pattern as existing providers
- Web UI includes Settings panel for managing A2A agents with discover/register/remove capabilities

**Lessons Learned:**
- A2A implementation required 8 distinct layers working together systematically to maintain clean architecture
- Event validation and TypeCheck were critical checkpoints to catch issues early
- Pre-existing errors in ChatView.browser.tsx are isolated to Playwright and don't affect new work

**Action Items:**
- Test manually: start Bird Code, navigate to Settings → A2A Agents
- Set environment variables: `A2A_INBOUND_AUTH_TOKEN` (inbound), `A2A_AUTH_TOKEN` (outbound)
- Test with external A2A agents; test internal delegation with provider `"a2a"`
- Verify: ProviderRuntimeEvent type mapping matches actual event payloads during testing
- Verify: SSE streaming parser handles remote agent format quirks
- Verify: TanStack Router file-based routing auto-generates `/settings/a2a` route
- Note: 95% confidence in completeness; 5% uncertainty centered on event payload shapes and SSE parsing edge cases

